export const placeOrder = async (req, res) => {
  const { product_id, quantity } = req.body;
  // address will take from then user address
};

// name ====== email ===== product_id ============ address ==== quantiy ==== paided ====== total (rs) ===== number === date === bill pdf link
// mehul       zyx.com      21212 (onClick          1)dc de
//                          product details)        2) dcwe
//
